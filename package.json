{
  "private": true,
  "scripts": {
    "pre-commit": "cd frontend && yarn lint-staged",
    "postinstall": "husky install && cd frontend && yarn"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.4.4",
    "@commitlint/config-conventional": "^18.4.4",
    "husky": "^8.0.3"
  }
}
