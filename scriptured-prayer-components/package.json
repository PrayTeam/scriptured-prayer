{
  "name": "scriptured-prayer-components",
  "version": "1.0.0",
  "scripts": {
    "serve": "node serve",
    "dev": "node esbuild.mjs dev --watch",
    "build": "rimraf ../prayerapp/static/compiled/* && node esbuild.mjs build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "esbuild": "0.19.10",
    "express": "^4.18.2",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
